simpleBtn = "//button[.='Simple']";



Given("Run the appium.exe file", function ()
{
  var launchApplication =  TestedApps.Appium.Run(); 
});

Then("Maximize the appium window", function ()
{
  Action.maximizeWindow();
});

Then("Wait until the simple button is visible", function ()
{
  Action.waitForElement(simpleBtn);
});




Given("close the application", function ()
{
  Sys.Process("Appium").BrowserWindow(0).Close();
});
